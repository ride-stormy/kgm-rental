# Design: {{featureName}}

## Executive Summary

| 항목 | 내용 |
|------|------|
| Feature | {{featureName}} |
| Epic | {{epicName}} |
| 작성일 | {{date}} |
| 프로필 | {{profileName}} |
| Plan 참조 | `docs/01-plan/{{epicName}}.plan.md` |

---

## 1. 아키텍처 옵션

### Option A — Minimal

(최소 변경, 빠른 구현)

### Option B — Clean Architecture

(완전한 분리, 최고 유지보수성)

### Option C — Pragmatic Balance (권장)

(실용적 균형)

### 옵션 비교

| 기준 | Option A | Option B | Option C |
|------|:--------:|:--------:|:--------:|
| 구현 속도 | ★★★★★ | ★★ | ★★★★ |
| 유지보수성 | ★★ | ★★★★★ | ★★★★ |
| 확장성 | ★★ | ★★★★★ | ★★★★ |
| 리스크 | 낮음 | 높음 | 중간 |

**선택: Option _** (사용자 결정)

---

## 2. 상세 설계

### 2.1 디렉토리 구조

```
(선택된 옵션의 디렉토리 구조)
```

### 2.2 핵심 컴포넌트

| 컴포넌트 | 역할 | 파일 |
|---------|------|------|
| (컴포넌트) | (역할) | (파일 경로) |

### 2.3 데이터 흐름

(데이터가 어떻게 흐르는지)

## 3. 구현 순서

| 순서 | 파일 | 유형 | 의존성 |
|------|------|------|--------|
| 1 | (파일) | 신규/수정 | — |

## 4. 인터페이스 명세

(API, 함수 시그니처, 데이터 스키마 등)

## 5. 검증 기준 (Acceptance Criteria)

> Plan의 Feature별 검증 기준을 구현 수준으로 구체화한다.
> CHECK 단계에서 functional 관점의 평가 기준으로 사용된다.

### 5.1 API 검증

| # | 엔드포인트 | 입력 | 기대 결과 |
|---|-----------|------|-----------|
| 1 | (예: GET /api/dealers) | (파라미터) | (응답 형태) |

### 5.2 UI 검증

| # | 화면/컴포넌트 | 동작 | 기대 결과 |
|---|-------------|------|-----------|
| 1 | (예: DealerList) | (사용자 동작) | (화면에 보이는 결과) |

### 5.3 엣지케이스

| # | 상황 | 기대 동작 |
|---|------|-----------|
| 1 | (예: 빈 목록) | (예: "등록된 딜러가 없습니다" 표시) |

### 5.4 런타임 연동 검증 (외부 서비스 연동 시 필수)

> 외부 서비스(Supabase, Firebase, 외부 API 등)를 사용하는 Feature는 이 섹션을 반드시 작성한다.
> Check 단계에서 dev 서버 기동 + 실제 API 호출로 검증한다.

| # | 서비스 | 검증 항목 | 확인 방법 |
|---|--------|-----------|----------|
| 1 | (예: Supabase) | RLS 정책 적용 — 인증된 사용자만 접근 가능 | dev 서버 기동 후 curl로 인증/비인증 호출 비교 |
| 2 | (예: Supabase) | 환경변수 설정 — SUPABASE_URL, SUPABASE_ANON_KEY | `.env` 파일 존재 및 값 확인 |
| 3 | (예: Supabase) | 정상 응답 반환 — 200/201, 오류 없음 | 실제 API 호출 응답 확인 |

---

*이 문서는 `/harness:design` 단계의 산출물입니다.*
*다음 단계: `/harness:do`*
